import { PanelPlaceholder } from './PanelPlaceholder';

/** Left column (~250px): component palette. */
export function LeftPanel() {
  return <PanelPlaceholder title="Komponensek" hint="Bal panel jön ide" />;
}
