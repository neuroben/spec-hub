import { PanelPlaceholder } from './PanelPlaceholder';

/** Right column (~300px): inspector for the selected module / component. */
export function InspectorPanel() {
  return <PanelPlaceholder title="Beállítások" hint="Jobb panel jön ide" />;
}
